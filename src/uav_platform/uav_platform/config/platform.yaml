platform_interface:
  ros__parameters:
    max_linear_speed: 3.0
    max_vertical_speed: 2.0
    takeoff_altitude_threshold: 0.3
    landing_altitude_threshold: 0.1

gazebo_driver:
  ros__parameters:
    # shared driver params later